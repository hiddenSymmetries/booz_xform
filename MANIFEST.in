include README.md LICENSE
graft src
graft externalPackages
global-include CMakeLists.txt *.cmake *.cpp *.hpp *.h

# Keep an sdist built from a working tree (as opposed to a clean checkout) from
# picking up editor backups and previous in-place builds.
global-exclude *~ *.so *.dylib *.o *.a *.pyc
